- provide(:title,'Edit Profile')
- provide(:button, 'Save changes')
%h1 Edit your profile
.row
  .span4
    = if @user.avatar?                         |
         image_tag @user.avatar.url(:medium)   |
      else                                     |
         image_tag "missing.jpeg", size: "300" |
      end                                      |
  .span6.offset0
    = render 'user_form'
