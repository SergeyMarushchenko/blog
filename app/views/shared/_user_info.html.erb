  <%= 
     if current_user.avatar?
        image_tag current_user.avatar.url(:medium)
     else
        image_tag "missing.jpeg", size: "300"
     end
  %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <h1><%= link_to "My Blog", current_user %></h1>
</span>
<span>
  <%= pluralize(current_user.posts.count, "post") %>
</span>
